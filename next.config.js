/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // assetPrefix: './',
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  // unoptimized: false,
};

module.exports = nextConfig;
